// Package library declares the Library value type used to represent a
// Plex library (Movies, TV, Music, etc.) along with the pure
// classification helpers (IsType, ContentTypeLabel, Build,
// ItemCountTypes). It depends only on the plexapi library's wire types; all
// behaviour here is deterministic and side-effect free.
package library
